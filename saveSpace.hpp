#ifndef SAVESPACE_HPP
#define SAVESPACE_HPP

/*#include <cstdint>
#include <cstddef>


extern uint8_t g_save_area[131072];
// helpers ultra simples
template<typename T>
inline T& save_at(std::size_t offset)
{
    return *reinterpret_cast<T*>(g_save_area + offset);
}*/

/*#include <cstdint>

extern "C" {
    extern uint8_t g_save_area[131072];
}

    void save_load();
    void save_flush();*/

#endif // SAVESPACE_HPP
